function PlaceholderPage({ title, description }) {
  return <section className="placeholder-view" aria-labelledby="placeholder-title"><p className="eyebrow">STUDYFLOW</p><h2 id="placeholder-title">{title}</h2><p>{description}</p></section>
}

export default PlaceholderPage
